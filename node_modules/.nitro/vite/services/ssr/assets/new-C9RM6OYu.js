import { r as createProject } from "./store-BjAP6kxP.js";
import { i as emptyProjectInput } from "./projects-Dck77_K-.js";
import { t as ProjectForm } from "./project-form-BJ1lRR-B.js";
import { useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
//#region src/routes/admin/projects/new.tsx?tsr-split=component
function NewProject() {
	const navigate = useNavigate();
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx("p", {
			className: "label-tech",
			children: "Selected Work"
		}),
		/* @__PURE__ */ jsx("h1", {
			className: "mt-3 font-display text-4xl font-semibold tracking-[-0.035em]",
			children: "New project"
		}),
		/* @__PURE__ */ jsx("p", {
			className: "mt-4 max-w-xl text-sm leading-relaxed text-quiet",
			children: "Content only. Imagery is handled by the public visual system — do not invent clients, quotes, or results."
		}),
		/* @__PURE__ */ jsx("div", {
			className: "mt-10",
			children: /* @__PURE__ */ jsx(ProjectForm, {
				initial: emptyProjectInput(),
				submitLabel: "Create project",
				onCancel: () => navigate({ to: "/admin/projects" }),
				onSubmit: async (input) => {
					try {
						const payload = {
							title: input.title,
							slug: input.slug,
							client: input.client,
							category: input.category,
							short_description: input.description,
							overview: input.overview,
							challenge: input.challenge,
							approach: input.approach,
							design: input.design,
							development: input.development,
							results: input.results,
							cover_image: input.cover_image ?? null,
							services: input.services,
							technologies: input.technologies,
							project_link: input.url || null,
							status: input.status === "live" ? "PUBLISHED" : "DRAFT"
						};
						await createProject(payload);
						toast("Project created.");
						navigate({ to: "/admin/projects" });
					} catch (err) {
						toast("Failed to create project.");
						navigate({ to: "/admin/projects" });
					}
				}
			})
		})
	] });
}
//#endregion
export { NewProject as component };
