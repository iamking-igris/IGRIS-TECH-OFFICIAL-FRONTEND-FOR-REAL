import { t as useContentStore } from "./store-BjAP6kxP.js";
//#region src/lib/content/contact.ts
var PROJECT_TYPES = [
	"Website",
	"Web Application",
	"Software",
	"AI Solution",
	"Automation",
	"Other"
];
var BUDGETS = [
	"Prefer not to say",
	"Under $2,000",
	"$2,000 – $8,000",
	"$8,000 – $20,000",
	"$20,000+"
];
var TIMELINES = [
	"As soon as possible",
	"1–2 months",
	"3–6 months",
	"Flexible"
];
var CONTACT_METHODS = [
	"Email",
	"Phone",
	"WhatsApp",
	"Other"
];
/**
* Contact / inquiry repository.
* Mock capture only — no email is sent.
* Later: POST /api/contact.
*/
var contactService = {
	list() {
		return useContentStore.getState().inquiries;
	},
	submit(input) {
		const name = input.name.trim();
		const email = input.email.trim();
		const type = input.type.trim();
		const description = input.description.trim();
		if (!name) throw new Error("Name is required.");
		if (!email) throw new Error("Email is required.");
		if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) throw new Error("Enter a valid email address.");
		if (!type) throw new Error("Project type is required.");
		if (!description) throw new Error("Project description is required.");
		return useContentStore.getState().createInquiry(input);
	},
	setRead(id, read) {
		useContentStore.getState().setInquiryRead(id, read);
	},
	delete(id) {
		useContentStore.getState().deleteInquiry(id);
	}
};
function useInquiries() {
	return useContentStore((s) => s.inquiries);
}
//#endregion
export { contactService as a, TIMELINES as i, CONTACT_METHODS as n, useInquiries as o, PROJECT_TYPES as r, BUDGETS as t };
