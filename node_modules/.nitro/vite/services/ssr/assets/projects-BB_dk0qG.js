import { n as ButtonLink, t as Button } from "./button-link-DuD0WsTl.js";
import { o as projectsService, r as PROJECT_STATUSES, u as useProjects } from "./projects-Dck77_K-.js";
import { l as useContentHydrated } from "./router-RsAy9zmm.js";
import { a as StatusMark, s as TextInput, t as AdminEmpty } from "./fields-BhPnEFGH.js";
import { n as AdminPending } from "./admin-gate-BQd-UODf.js";
import { t as ConfirmDialog } from "./confirm-dialog-BPb7esTL.js";
import { useMemo, useState } from "react";
import { Link, useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
//#region src/routes/admin/projects/index.tsx?tsr-split=component
function ProjectsIndex() {
	const hydrated = useContentHydrated();
	const projects = useProjects();
	const navigate = useNavigate();
	const [query, setQuery] = useState("");
	const [pendingDelete, setPendingDelete] = useState(null);
	const filtered = useMemo(() => {
		const q = query.trim().toLowerCase();
		if (!q) return projects;
		return projects.filter((p) => [
			p.title,
			p.client,
			p.category,
			p.slug
		].join(" ").toLowerCase().includes(q));
	}, [projects, query]);
	if (!hydrated) return /* @__PURE__ */ jsx(AdminPending, { children: "Loading projects" });
	const target = projects.find((p) => p.id === pendingDelete);
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsxs("div", {
			className: "flex flex-col gap-6 md:flex-row md:items-end md:justify-between",
			children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
				className: "label-tech",
				children: "Selected Work"
			}), /* @__PURE__ */ jsx("h1", {
				className: "mt-3 font-display text-4xl font-semibold tracking-[-0.035em]",
				children: "Projects"
			})] }), /* @__PURE__ */ jsx(ButtonLink, {
				to: "/admin/projects/new",
				variant: "primary",
				children: "New project"
			})]
		}),
		/* @__PURE__ */ jsx("div", {
			className: "mt-10 max-w-sm",
			children: /* @__PURE__ */ jsx(TextInput, {
				value: query,
				onChange: (e) => setQuery(e.target.value),
				placeholder: "Search title, client, category",
				"aria-label": "Search projects"
			})
		}),
		filtered.length === 0 ? /* @__PURE__ */ jsx(AdminEmpty, {
			title: projects.length === 0 ? "No projects yet." : "No matches.",
			body: projects.length === 0 ? "Add a case study when there is real work to publish. Do not invent clients or results." : "Try a different search.",
			action: projects.length === 0 ? /* @__PURE__ */ jsx(ButtonLink, {
				to: "/admin/projects/new",
				variant: "ghost",
				children: "New project"
			}) : void 0
		}) : /* @__PURE__ */ jsx("ul", {
			className: "mt-10",
			children: filtered.map((project, i) => /* @__PURE__ */ jsxs("li", {
				className: "grid gap-4 border-t border-hairline py-6 last:border-b md:grid-cols-12 md:items-center",
				children: [
					/* @__PURE__ */ jsx("p", {
						className: "font-mono text-[10px] tracking-widest text-faint md:col-span-1",
						children: String(i + 1).padStart(2, "0")
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "md:col-span-4",
						children: [/* @__PURE__ */ jsx("p", {
							className: "font-display text-xl font-semibold tracking-[-0.03em]",
							children: project.title
						}), /* @__PURE__ */ jsxs("p", {
							className: "mt-1 text-sm text-quiet",
							children: [
								project.client,
								" · ",
								project.category
							]
						})]
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "flex flex-wrap gap-2 md:col-span-3",
						children: [/* @__PURE__ */ jsx(StatusMark, {
							tone: project.published ? "ink" : "quiet",
							children: project.published ? "PUBLISHED" : "DRAFT"
						}), /* @__PURE__ */ jsx(StatusMark, { children: PROJECT_STATUSES.find((s) => s.value === project.status)?.label.toUpperCase() ?? project.status.toUpperCase() })]
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "flex flex-wrap gap-2 md:col-span-4 md:justify-end",
						children: [
							/* @__PURE__ */ jsx(Button, {
								variant: "ghost",
								className: "h-10 min-h-10 px-3 text-xs",
								onClick: () => navigate({
									to: "/admin/projects/$id",
									params: { id: project.id }
								}),
								children: "Edit"
							}),
							/* @__PURE__ */ jsx(Button, {
								variant: "ghost",
								className: "h-10 min-h-10 px-3 text-xs",
								onClick: () => {
									projectsService.setPublished(project.id, !project.published);
									toast(project.published ? "Unpublished from the public site." : "Published on the public site.");
								},
								children: project.published ? "Unpublish" : "Publish"
							}),
							project.published ? /* @__PURE__ */ jsx(Link, {
								to: "/work/$slug",
								params: { slug: project.slug },
								className: "inline-flex h-10 items-center px-3 text-xs text-quiet hover:text-ink",
								children: "View"
							}) : null,
							/* @__PURE__ */ jsx(Button, {
								variant: "ghost",
								className: "h-10 min-h-10 px-3 text-xs text-danger",
								onClick: () => setPendingDelete(project.id),
								children: "Delete"
							})
						]
					})
				]
			}, project.id))
		}),
		/* @__PURE__ */ jsx(ConfirmDialog, {
			open: Boolean(target),
			title: "Delete this project?",
			body: target ? `“${target.title}” will be removed from the mock repository in this browser. This cannot be undone here.` : "",
			confirmLabel: "Delete project",
			onCancel: () => setPendingDelete(null),
			onConfirm: () => {
				if (target) {
					projectsService.delete(target.id);
					toast("Project deleted.");
				}
				setPendingDelete(null);
			}
		})
	] });
}
//#endregion
export { ProjectsIndex as component };
