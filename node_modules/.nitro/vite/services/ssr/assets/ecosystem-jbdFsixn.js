import { t as Button } from "./button-link-DuD0WsTl.js";
import { d as ecosystemService, f as ecosystemToInput, l as useContentHydrated, m as useEcosystem, p as emptyEcosystemInput, u as ECOSYSTEM_STATUSES } from "./router-RsAy9zmm.js";
import { a as StatusMark, i as SelectInput, n as CheckRow, o as TextArea, r as Field, s as TextInput, t as AdminEmpty } from "./fields-BhPnEFGH.js";
import { n as AdminPending } from "./admin-gate-BQd-UODf.js";
import { t as ConfirmDialog } from "./confirm-dialog-BPb7esTL.js";
import { useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
//#region src/routes/admin/ecosystem.tsx?tsr-split=component
function EcosystemAdmin() {
	const hydrated = useContentHydrated();
	const items = useEcosystem();
	const [editing, setEditing] = useState(null);
	const [pendingDelete, setPendingDelete] = useState(null);
	if (!hydrated) return /* @__PURE__ */ jsx(AdminPending, { children: "Loading ecosystem" });
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsxs("div", {
			className: "flex flex-col gap-6 md:flex-row md:items-end md:justify-between",
			children: [/* @__PURE__ */ jsxs("div", { children: [
				/* @__PURE__ */ jsx("p", {
					className: "label-tech",
					children: "Products"
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "mt-3 font-display text-4xl font-semibold tracking-[-0.035em]",
					children: "Ecosystem"
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-4 max-w-xl text-sm leading-relaxed text-quiet",
					children: "Name, details, link, status. Keep it small — this is not a CMS for marketing pages."
				})
			] }), /* @__PURE__ */ jsx(Button, {
				variant: "primary",
				onClick: () => setEditing("new"),
				arrow: true,
				children: "Add product"
			})]
		}),
		editing ? /* @__PURE__ */ jsxs("div", {
			className: "mt-10 border-t border-hairline pt-8",
			children: [/* @__PURE__ */ jsx("h2", {
				className: "font-display text-2xl font-semibold tracking-[-0.03em]",
				children: editing === "new" ? "New product" : `Edit ${editing.name}`
			}), /* @__PURE__ */ jsx("div", {
				className: "mt-8",
				children: /* @__PURE__ */ jsx(EcosystemForm, {
					initial: editing === "new" ? {
						...emptyEcosystemInput(),
						order: items.length + 1
					} : ecosystemToInput(editing),
					submitLabel: editing === "new" ? "Create product" : "Save product",
					onCancel: () => setEditing(null),
					onSubmit: (input) => {
						if (editing === "new") {
							ecosystemService.create(input);
							toast("Ecosystem product created.");
						} else {
							ecosystemService.update(editing.id, input);
							toast("Ecosystem product saved.");
						}
						setEditing(null);
					}
				})
			})]
		}) : null,
		items.length === 0 ? /* @__PURE__ */ jsx(AdminEmpty, {
			title: "No ecosystem products yet.",
			body: "Add IGRIS Hosting, IGRIS Studio, or whatever comes next."
		}) : /* @__PURE__ */ jsx("ul", {
			className: "mt-10",
			children: items.map((item, i) => /* @__PURE__ */ jsxs("li", {
				className: "grid gap-4 border-t border-hairline py-6 last:border-b md:grid-cols-12 md:items-center",
				children: [
					/* @__PURE__ */ jsx("p", {
						className: "font-mono text-[10px] tracking-widest text-faint md:col-span-1",
						children: String(i + 1).padStart(2, "0")
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "md:col-span-5",
						children: [
							/* @__PURE__ */ jsx("p", {
								className: "font-display text-xl font-semibold tracking-[-0.03em]",
								children: item.name
							}),
							/* @__PURE__ */ jsx("p", {
								className: "mt-1 text-sm text-quiet",
								children: item.description
							}),
							/* @__PURE__ */ jsx("p", {
								className: "mt-2 font-mono text-[10px] tracking-wider text-faint",
								children: item.url ?? item.subdomain
							})
						]
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "flex flex-wrap gap-2 md:col-span-3",
						children: [/* @__PURE__ */ jsx(StatusMark, {
							tone: item.published ? "ink" : "quiet",
							children: item.published ? "PUBLISHED" : "HIDDEN"
						}), /* @__PURE__ */ jsx(StatusMark, { children: item.status.toUpperCase() })]
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "flex flex-wrap gap-2 md:col-span-3 md:justify-end",
						children: [/* @__PURE__ */ jsx(Button, {
							variant: "ghost",
							className: "h-10 min-h-10 px-3 text-xs",
							onClick: () => setEditing(item),
							children: "Edit"
						}), /* @__PURE__ */ jsx(Button, {
							variant: "ghost",
							className: "h-10 min-h-10 px-3 text-xs text-danger",
							onClick: () => setPendingDelete(item),
							children: "Delete"
						})]
					})
				]
			}, item.id))
		}),
		/* @__PURE__ */ jsx(ConfirmDialog, {
			open: Boolean(pendingDelete),
			title: "Delete this product?",
			body: pendingDelete ? `“${pendingDelete.name}” will be removed from the ecosystem list in this browser.` : "",
			confirmLabel: "Delete product",
			onCancel: () => setPendingDelete(null),
			onConfirm: () => {
				if (pendingDelete) {
					ecosystemService.delete(pendingDelete.id);
					toast("Ecosystem product deleted.");
					if (editing !== "new" && editing?.id === pendingDelete.id) setEditing(null);
				}
				setPendingDelete(null);
			}
		})
	] });
}
function EcosystemForm({ initial, submitLabel, onSubmit, onCancel }) {
	const [form, setForm] = useState(initial);
	const [error, setError] = useState("");
	function submit(e) {
		e.preventDefault();
		if (!form.name.trim()) {
			setError("Name is required.");
			return;
		}
		setError("");
		onSubmit(form);
	}
	return /* @__PURE__ */ jsxs("form", {
		onSubmit: submit,
		className: "grid max-w-xl gap-6",
		noValidate: true,
		children: [
			/* @__PURE__ */ jsx(Field, {
				label: "Name",
				htmlFor: "eco-name",
				children: /* @__PURE__ */ jsx(TextInput, {
					id: "eco-name",
					value: form.name,
					onChange: (e) => setForm({
						...form,
						name: e.target.value
					}),
					required: true
				})
			}),
			/* @__PURE__ */ jsx(Field, {
				label: "Details",
				htmlFor: "eco-details",
				children: /* @__PURE__ */ jsx(TextArea, {
					id: "eco-details",
					value: form.description,
					onChange: (e) => setForm({
						...form,
						description: e.target.value
					})
				})
			}),
			/* @__PURE__ */ jsx(Field, {
				label: "Link",
				htmlFor: "eco-url",
				hint: "(optional until live)",
				children: /* @__PURE__ */ jsx(TextInput, {
					id: "eco-url",
					type: "url",
					placeholder: "https://",
					value: form.url,
					onChange: (e) => setForm({
						...form,
						url: e.target.value
					})
				})
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-6 md:grid-cols-2",
				children: [/* @__PURE__ */ jsx(Field, {
					label: "Status",
					htmlFor: "eco-status",
					children: /* @__PURE__ */ jsx(SelectInput, {
						id: "eco-status",
						value: form.status,
						onChange: (e) => setForm({
							...form,
							status: e.target.value
						}),
						children: ECOSYSTEM_STATUSES.map((s) => /* @__PURE__ */ jsx("option", {
							value: s,
							children: s
						}, s))
					})
				}), /* @__PURE__ */ jsx(Field, {
					label: "Display order",
					htmlFor: "eco-order",
					children: /* @__PURE__ */ jsx(TextInput, {
						id: "eco-order",
						type: "number",
						value: form.order,
						onChange: (e) => setForm({
							...form,
							order: Number(e.target.value) || 0
						})
					})
				})]
			}),
			/* @__PURE__ */ jsx(CheckRow, {
				checked: form.published,
				onChange: (published) => setForm({
					...form,
					published
				}),
				children: "Published on the public site"
			}),
			error ? /* @__PURE__ */ jsx("p", {
				className: "text-sm text-danger",
				role: "alert",
				children: error
			}) : null,
			/* @__PURE__ */ jsxs("div", {
				className: "flex flex-wrap gap-3",
				children: [/* @__PURE__ */ jsx(Button, {
					type: "submit",
					variant: "primary",
					arrow: true,
					children: submitLabel
				}), /* @__PURE__ */ jsx(Button, {
					type: "button",
					variant: "ghost",
					onClick: onCancel,
					children: "Cancel"
				})]
			})
		]
	});
}
//#endregion
export { EcosystemAdmin as component };
