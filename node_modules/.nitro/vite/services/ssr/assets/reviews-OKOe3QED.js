import { t as cn } from "./utils-C_uf36nf.js";
import { t as Button } from "./button-link-DuD0WsTl.js";
import { i as useReviews, n as reviewsService } from "./reviews-BD-L5kxC.js";
import { r as reviewDisplayStatus, t as formatDate } from "./labels-Bn3OGMpp.js";
import { l as useContentHydrated } from "./router-ZY7VAfYm.js";
import { a as StatusMark, o as TextArea, r as Field, s as TextInput, t as AdminEmpty } from "./fields-BhPnEFGH.js";
import { n as AdminPending } from "./admin-gate-CFJKXS2i.js";
import { t as ConfirmDialog } from "./confirm-dialog-BPb7esTL.js";
import { useMemo, useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
//#region src/routes/admin/reviews.tsx?tsr-split=component
var FILTERS = [
	{
		id: "all",
		label: "All"
	},
	{
		id: "pending",
		label: "Pending"
	},
	{
		id: "approved",
		label: "Approved"
	},
	{
		id: "published",
		label: "Published"
	},
	{
		id: "rejected",
		label: "Rejected"
	}
];
function toneFor(status) {
	if (status === "published") return "ink";
	if (status === "rejected") return "danger";
	return "quiet";
}
function ReviewsAdmin() {
	const hydrated = useContentHydrated();
	const reviews = useReviews();
	const [filter, setFilter] = useState("all");
	const [editing, setEditing] = useState(null);
	const [pendingDelete, setPendingDelete] = useState(null);
	const visible = useMemo(() => {
		if (filter === "all") return reviews;
		return reviews.filter((r) => reviewDisplayStatus(r) === filter);
	}, [reviews, filter]);
	if (!hydrated) return /* @__PURE__ */ jsx(AdminPending, { children: "Loading reviews" });
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx("p", {
			className: "label-tech",
			children: "Moderation"
		}),
		/* @__PURE__ */ jsx("h1", {
			className: "mt-3 font-display text-4xl font-semibold tracking-[-0.035em]",
			children: "Reviews"
		}),
		/* @__PURE__ */ jsx("p", {
			className: "mt-4 max-w-xl text-sm leading-relaxed text-quiet",
			children: "Public submissions arrive as pending. Only approved, published notes appear on the site."
		}),
		/* @__PURE__ */ jsx("div", {
			className: "mt-8 flex flex-wrap gap-2",
			role: "tablist",
			"aria-label": "Filter reviews",
			children: FILTERS.map((item) => /* @__PURE__ */ jsx("button", {
				type: "button",
				role: "tab",
				"aria-selected": filter === item.id,
				onClick: () => setFilter(item.id),
				className: cn("h-10 border px-3 text-xs tracking-wide transition-colors duration-150", filter === item.id ? "border-ink bg-ink text-canvas" : "border-hairline text-quiet hover:border-quiet"),
				children: item.label
			}, item.id))
		}),
		editing ? /* @__PURE__ */ jsxs("div", {
			className: "mt-10 border-t border-hairline pt-8",
			children: [/* @__PURE__ */ jsx("h2", {
				className: "font-display text-2xl font-semibold tracking-[-0.03em]",
				children: "Edit review"
			}), /* @__PURE__ */ jsx("div", {
				className: "mt-8",
				children: /* @__PURE__ */ jsx(ReviewEditForm, {
					review: editing,
					onCancel: () => setEditing(null),
					onSave: (patch) => {
						reviewsService.update(editing.id, patch);
						toast("Review saved.");
						setEditing(null);
					}
				})
			})]
		}) : null,
		visible.length === 0 ? /* @__PURE__ */ jsx(AdminEmpty, {
			title: "No reviews in this view.",
			body: "When a client leaves a note, it will wait here until it is approved."
		}) : /* @__PURE__ */ jsx("ul", {
			className: "mt-10",
			children: visible.map((review) => {
				const status = reviewDisplayStatus(review);
				return /* @__PURE__ */ jsx("li", {
					className: "border-t border-hairline py-6 last:border-b",
					children: /* @__PURE__ */ jsxs("div", {
						className: "flex flex-wrap items-start justify-between gap-4",
						children: [/* @__PURE__ */ jsxs("div", {
							className: "max-w-2xl",
							children: [
								/* @__PURE__ */ jsxs("div", {
									className: "flex flex-wrap items-center gap-3",
									children: [/* @__PURE__ */ jsx(StatusMark, {
										tone: toneFor(status),
										children: status.toUpperCase()
									}), /* @__PURE__ */ jsxs("span", {
										className: "font-mono text-[10px] tracking-widest text-faint",
										children: [formatDate(review.createdAt), review.rating ? ` · ${review.rating}/5` : ""]
									})]
								}),
								/* @__PURE__ */ jsx("p", {
									className: "mt-4 text-base leading-relaxed text-ink",
									children: review.content
								}),
								/* @__PURE__ */ jsxs("p", {
									className: "mt-3 text-sm text-quiet",
									children: [
										review.clientName,
										review.role ? `, ${review.role}` : "",
										review.company ? ` — ${review.company}` : "",
										review.project ? ` · ${review.project}` : ""
									]
								})
							]
						}), /* @__PURE__ */ jsxs("div", {
							className: "flex flex-wrap gap-2",
							children: [
								review.status === "pending" ? /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(Button, {
									variant: "primary",
									className: "h-10 min-h-10 px-3 text-xs",
									onClick: () => {
										reviewsService.approve(review.id);
										toast("Review approved and published.");
									},
									children: "Approve"
								}), /* @__PURE__ */ jsx(Button, {
									variant: "ghost",
									className: "h-10 min-h-10 px-3 text-xs",
									onClick: () => {
										reviewsService.reject(review.id);
										toast("Review rejected.");
									},
									children: "Reject"
								})] }) : null,
								review.status === "approved" ? /* @__PURE__ */ jsx(Button, {
									variant: "ghost",
									className: "h-10 min-h-10 px-3 text-xs",
									onClick: () => {
										reviewsService.setPublished(review.id, !review.published);
										toast(review.published ? "Unpublished from the public site." : "Published on the public site.");
									},
									children: review.published ? "Unpublish" : "Publish"
								}) : null,
								review.status === "rejected" ? /* @__PURE__ */ jsx(Button, {
									variant: "ghost",
									className: "h-10 min-h-10 px-3 text-xs",
									onClick: () => {
										reviewsService.approve(review.id);
										toast("Review approved and published.");
									},
									children: "Approve"
								}) : null,
								/* @__PURE__ */ jsx(Button, {
									variant: "ghost",
									className: "h-10 min-h-10 px-3 text-xs",
									onClick: () => setEditing(review),
									children: "Edit"
								}),
								/* @__PURE__ */ jsx(Button, {
									variant: "ghost",
									className: "h-10 min-h-10 px-3 text-xs text-danger",
									onClick: () => setPendingDelete(review),
									children: "Delete"
								})
							]
						})]
					})
				}, review.id);
			})
		}),
		/* @__PURE__ */ jsx(ConfirmDialog, {
			open: Boolean(pendingDelete),
			title: "Delete this review?",
			body: "The note will be removed from the mock repository in this browser.",
			confirmLabel: "Delete review",
			onCancel: () => setPendingDelete(null),
			onConfirm: () => {
				if (pendingDelete) {
					reviewsService.delete(pendingDelete.id);
					toast("Review deleted.");
					if (editing?.id === pendingDelete.id) setEditing(null);
				}
				setPendingDelete(null);
			}
		})
	] });
}
function ReviewEditForm({ review, onSave, onCancel }) {
	const [clientName, setClientName] = useState(review.clientName);
	const [company, setCompany] = useState(review.company);
	const [role, setRole] = useState(review.role);
	const [content, setContent] = useState(review.content);
	const [project, setProject] = useState(review.project ?? "");
	const [error, setError] = useState("");
	function submit(e) {
		e.preventDefault();
		if (!clientName.trim() || !content.trim()) {
			setError("Name and review are required.");
			return;
		}
		onSave({
			clientName: clientName.trim(),
			company: company.trim(),
			role: role.trim(),
			content: content.trim(),
			project: project.trim() || null
		});
	}
	return /* @__PURE__ */ jsxs("form", {
		onSubmit: submit,
		className: "grid max-w-xl gap-6",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-6 md:grid-cols-2",
				children: [/* @__PURE__ */ jsx(Field, {
					label: "Name",
					htmlFor: "rev-name",
					children: /* @__PURE__ */ jsx(TextInput, {
						id: "rev-name",
						value: clientName,
						onChange: (e) => setClientName(e.target.value)
					})
				}), /* @__PURE__ */ jsx(Field, {
					label: "Company",
					htmlFor: "rev-company",
					children: /* @__PURE__ */ jsx(TextInput, {
						id: "rev-company",
						value: company,
						onChange: (e) => setCompany(e.target.value)
					})
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "grid gap-6 md:grid-cols-2",
				children: [/* @__PURE__ */ jsx(Field, {
					label: "Role",
					htmlFor: "rev-role",
					children: /* @__PURE__ */ jsx(TextInput, {
						id: "rev-role",
						value: role,
						onChange: (e) => setRole(e.target.value)
					})
				}), /* @__PURE__ */ jsx(Field, {
					label: "Related work",
					htmlFor: "rev-project",
					children: /* @__PURE__ */ jsx(TextInput, {
						id: "rev-project",
						value: project,
						onChange: (e) => setProject(e.target.value)
					})
				})]
			}),
			/* @__PURE__ */ jsx(Field, {
				label: "Review",
				htmlFor: "rev-content",
				children: /* @__PURE__ */ jsx(TextArea, {
					id: "rev-content",
					value: content,
					onChange: (e) => setContent(e.target.value)
				})
			}),
			error ? /* @__PURE__ */ jsx("p", {
				className: "text-sm text-danger",
				role: "alert",
				children: error
			}) : null,
			/* @__PURE__ */ jsxs("div", {
				className: "flex flex-wrap gap-3",
				children: [/* @__PURE__ */ jsx(Button, {
					type: "submit",
					variant: "primary",
					children: "Save review"
				}), /* @__PURE__ */ jsx(Button, {
					type: "button",
					variant: "ghost",
					onClick: onCancel,
					children: "Cancel"
				})]
			})
		]
	});
}
//#endregion
export { ReviewsAdmin as component };
